module.exports = {
  test: async (req, res) => {
    // const db = req.app.get("db");
    // const { string } = req.body;
    // let output = null;
    // // decode string
    // try {
    //   output = decodeString(string);
    // } catch {
    //   res.status(400).send("Invalid Factorio blueprint string");
    // }
    // // read first object
    // readNextObj(output);
    // res.status(200).send(output);
  }
};
